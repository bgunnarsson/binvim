#ifndef PLAYGROUND_UTIL_H
#define PLAYGROUND_UTIL_H

#include <stddef.h>

/* Returns the number of vowels in the first n bytes of s. */
size_t count_vowels(const char *s, size_t n);

#endif /* PLAYGROUND_UTIL_H */
